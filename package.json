{
  "name": "editor-peer-bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cross-editor peer bridge — release tooling root",
  "scripts": {
    "release": "node scripts/release.mjs",
    "release:vscode": "node scripts/release.mjs vscode",
    "release:rider": "node scripts/release.mjs rider",
    "release:dry": "node scripts/release.mjs --dry-run",
    "version:read": "node scripts/version.mjs read",
    "version:sync": "node scripts/version.mjs sync",
    "version:check": "node scripts/version.mjs check",
    "version:check-tag": "node scripts/version.mjs check-tag"
  }
}
